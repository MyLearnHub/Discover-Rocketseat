// RODAR NO NAVEGADOR PARA ACESSAR O DEV TOOLS

// O JavaScript possui protótipos para auxiliar na construção de elementos. Como o objeto
// A linguagem é baseada em protótipos, possuindo uma cadeia deles.
/* 
    Ao colocar '{}' no dev tools, e apertamos enter ele cria um objeto, 
    que já por padrão vem inicializado com o método __proto__, que dentro dele contém outros métodos. 
*/

// Esse encapsulamento ocorre para a maioria dos dados em JavaScript.
const tamanho = "Paulo".length
console.log(tamanho)