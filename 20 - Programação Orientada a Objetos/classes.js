// Syntactical sugar - Prototype
// JavaScript tem herança de métodos por baixo dos panos em uma nova classe

// Definir classe
class Pessoa {
    constructor() {
        this.id = ~~(Math.random() * 100000)
        this.name = nome
    }

    dizerNome() {
        console.log(this.name)
    }
}

// Criar objeto
const pessoa = new Pessoa('Maik');